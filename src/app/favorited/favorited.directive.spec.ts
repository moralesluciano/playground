import { FavoritedDirective } from './favorited.directive';

describe('FavoritedDirective', () => {
  it('should create an instance', () => {
    const directive = new FavoritedDirective();
    expect(directive).toBeTruthy();
  });
});
